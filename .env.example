# Claine v2 - Environment Variables
# Copy this file to .env and set appropriate values for your environment

# ============================================
# APPLICATION CONFIGURATION
# ============================================

# Application name displayed in the UI
# Used for branding and window titles
VITE_APP_NAME=Claine

# ============================================
# API CONFIGURATION
# ============================================

# Gmail API base URL
# Development: Use local mock server or Gmail API test endpoint
# Production: https://gmail.googleapis.com
# Note: Gmail API integration happens in Epic 1 (Story 1.4)
VITE_API_URL=https://gmail.googleapis.com

# ============================================
# OAUTH 2.0 CONFIGURATION (Story 1.4)
# ============================================

# Google OAuth 2.0 Client ID
# Get this from Google Cloud Console: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID → Application type: Web application
# Note: Client ID is safe to expose in client-side code
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Google OAuth 2.0 Client Secret
# When using PKCE with SPAs, client secret is less sensitive
# PKCE provides primary security, so it's acceptable in client code
# Still, treat with reasonable care
VITE_GOOGLE_CLIENT_SECRET=your-client-secret

# OAuth 2.0 Redirect URI
# Must match exactly what's configured in Google Cloud Console
# Development: http://localhost:5173/auth/callback
# Production: https://yourdomain.com/auth/callback
VITE_GOOGLE_REDIRECT_URI=http://localhost:5173/auth/callback

# OAuth 2.0 Scopes
# Gmail API scopes needed for email access
# Space-separated list
VITE_GOOGLE_SCOPES=https://www.googleapis.com/auth/gmail.readonly https://www.googleapis.com/auth/gmail.send

# ============================================
# OAUTH 2.0 CONFIGURATION - MICROSOFT OUTLOOK (Story 1.5)
# ============================================

# Microsoft OAuth 2.0 Client ID
# Get this from Azure Portal: https://portal.azure.com
# Navigate to: App registrations → New registration
# Application type: Web (SPA)
# Note: Client ID is safe to expose in client-side code
VITE_MICROSOFT_CLIENT_ID=your-client-id

# Microsoft OAuth 2.0 Client Secret
# When using PKCE with SPAs, client secret is less sensitive
# PKCE provides primary security, so it's acceptable in client code
# Still, treat with reasonable care
VITE_MICROSOFT_CLIENT_SECRET=your-client-secret

# OAuth 2.0 Redirect URI
# Must match exactly what's configured in Azure Portal
# Development: http://localhost:5173/auth/callback
# Production: https://yourdomain.com/auth/callback
VITE_MICROSOFT_REDIRECT_URI=http://localhost:5173/auth/callback

# OAuth 2.0 Scopes
# Microsoft Graph scopes needed for email access
# Space-separated list
# Note: offline_access is required to get refresh token
VITE_MICROSOFT_SCOPES=Mail.Read Mail.Send offline_access

# ============================================
# LOGGING & ERROR TRACKING (Story 1.14)
# ============================================

# Log level for console and IndexedDB logging
# Values: debug, info, warn, error
# Development default: debug (all logs)
# Production default: warn (only warnings and errors)
VITE_LOG_LEVEL=debug

# Sentry DSN for error tracking
# Get this from Sentry: https://sentry.io
# Create a React project → Settings → Client Keys (DSN)
# Leave empty to disable error tracking
VITE_SENTRY_DSN=

# ============================================
# SYNC CONFIGURATION (Story 1.15)
# ============================================

# Adaptive sync polling interval bounds
# When adaptive polling is enabled, intervals adjust between min and max
# based on account activity (new messages = shorter, idle = longer)
# Set to same value to effectively disable adaptive range
VITE_ADAPTIVE_MIN_INTERVAL_MS=60000
VITE_ADAPTIVE_MAX_INTERVAL_MS=600000

# ============================================
# DEVELOPMENT vs PRODUCTION EXAMPLES
# ============================================

# Development environment example:
# VITE_APP_NAME=Claine (Dev)
# VITE_API_URL=http://localhost:3001/api

# Production environment example:
# VITE_APP_NAME=Claine
# VITE_API_URL=https://gmail.googleapis.com

# ============================================
# NOTES
# ============================================

# - All client-side environment variables MUST use the VITE_ prefix
# - Variables are exposed to the browser bundle (do not put secrets here)
# - For local development, copy this file to .env and customize values
# - For production, set environment variables in your deployment platform (e.g., Vercel)
# - Server-side variables (build-time only) should NOT use VITE_ prefix

# ============================================
# DEPLOYMENT PLATFORM CONFIGURATION
# ============================================

# GitHub Actions:
# - Set secrets in: Repository Settings → Secrets and variables → Actions
# - Access in workflows via: ${{ secrets.SECRET_NAME }}

# Vercel:
# - Set environment variables in: Project Settings → Environment Variables
# - Configure per environment: Production, Preview, Development
# - See docs/stories/0-8-set-up-environment-variable-management.md for details
